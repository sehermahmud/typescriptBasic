// just need for example
const carMakers2 = ['lambogini', 'ford', 'toyota', 'chevy'];
const dates3 = [new Date(), new Date()];

const carsByMake3 = [['zizali']['Kai']['Sanena']['break']];

// TS can prevent us from adding incompatible values to the array

carMakers2.push('lusie'); // change the word "lusie" and replace with 100, and it will show an error
