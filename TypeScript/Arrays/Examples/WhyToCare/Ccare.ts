// just need for example
const carMakers3 = ['lambogini', 'ford', 'toyota', 'chevy'];
const dates4 = [new Date(), new Date()];

const carsByMake4 = [['zizali']['Kai']['Sanena']['break']];

// We can get help with 'map', 'forEach', 'reduce' functions

carMakers3.map((car: string): string => {
  return car;
});
