let TSBackForrestIceCream: number = 50;

let TSadoptMePets: string = 'mega neon, riding, flying, full grown unicon';

let TSbucks: boolean = true;

let TSscammers: null = null;

let TSfood: undefined = undefined;

// Built in object
let TSnow: Date = new Date();

// Arrays

let TScolors: string[] = ['Blue', 'Pink', 'Greenr'];

let TSluckyNumber: number[] = [13, 24];

let TStruths: boolean[] = [true, true, false];

// Functions

const TSlogNumber: (i: number) => void = (i: number) => {
  console.log(i);
};

// object literal

let TSpoint: { x: number; y: number } = {
  x: 10,
  y: 20,
};
